## プロジェクトを準備する

Salesforce の開発は **SFDX プロジェクト**（`sfdx-project.json` があるフォルダ）から始まります。

- 新規なら「新しいプロジェクトを作成」で雛形を生成
- 既存のリポジトリがあるなら、そのフォルダを VSCode で開くだけでOK

プロジェクトの中の `force-app/` に、Apex・LWC・オブジェクト定義などのソースが入ります。
