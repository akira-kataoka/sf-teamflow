/**
 * The team workflow guide, shown in-editor via the "ガイド" command. Kept as a
 * string constant (not a file read) so it works regardless of how the extension
 * is packaged. Mirrors docs/TEAM_WORKFLOW.md. Terminology matches the home
 * screen labels (資材作成 / 資材反映 / 資材取込 / バックアップ / 環境).
 */
export const TEAM_WORKFLOW_GUIDE = `# Salesforce チーム開発ガイド (Salesforce Dev Manager)

複数人で Salesforce を安全に開発するための進め方です。**初めての方はこの順番で読めば大丈夫です。**

## いちばん大事な区別 — 「資材反映」と「デプロイ」は別物

| 操作 | 何をする | 送り先 | いつ |
|------|----------|--------|------|
| **資材反映**（⬆️） | ローカルの資材を**自分の開発環境**へ直接送る | スクラッチ / 個人Sandbox | 開発中。何度直接送ってもOK |
| **環境へデプロイ**（🚀） | **共有環境**へ配布 | ステージング / 本番 | リリース時。git経由で |

> ⚠️ **スクラッチや自分の環境から、ステージング/本番へ「資材反映」で直接送ってはいけません。**
> 共有環境へは必ず次の道筋を通します（事故防止）：
>
> \`\`\`
> 資材反映で自分の環境で開発・確認
>    ↓ 💾 バックアップ (commit)
>    ↓ 🌿 ブランチ → 🔀 PR作成（レビュー）
>    ↓ ✅ 検証（お試し） / CI-CD で自動チェック
>    ↓ 🚀 環境へデプロイ（ステージング→本番）
> \`\`\`

## 全体像 — 3つの環境とブランチ

\`\`\`
   開発者A ─┐
   開発者B ─┼─▶ develop ブランチ ─▶ 統合(integration) Sandbox
   開発者C ─┘        │
                     ▼
              release/* ブランチ ─▶ ステージング(staging) Sandbox  … 受入テスト
                     │
                     ▼
                  main ブランチ ─▶ 本番(production)            … お客様が使う環境
\`\`\`

| 環境 | 役割 | ブランチ | 誰が触る |
|------|------|----------|----------|
| **開発 (dev/scratch)** | 個人が自由に試す | \`feature/*\` | 各開発者 |
| **統合 (integration)** | 全員の変更を合流して確認 | \`develop\` | 自動デプロイ |
| **ステージング (staging)** | 本番同等で受入テスト | \`release/*\` | QA・リーダー |
| **本番 (production)** | お客様が使う | \`main\` | リリース担当のみ |

> この対応は \`sf-teamflow.json\` に書いて全員で共有します。ホームの「① 環境設定」で編集でき、
> Salesforce Dev Manager が現在のブランチから自動で「どの環境か」を判断します。

## 日々の流れ (開発者)

1. **ブランチを作る** … ホームの「🌿 ブランチ管理」→ \`feature/account-search\` のように作成
   （1機能=1ブランチ。他の人の作業と混ざりません）。作成時に「この機能用のスクラッチも作成しますか？」
   と聞かれます → 作ると **1機能=1ブランチ=1使い捨て環境** に（ブランチ削除時に掃除も提案）。
2. **資材を作る** … 「② 開発」の「✨ 資材作成」で Apexクラス/トリガ/LWC/Aura を作成
3. **自分の環境で確認** … 「⬆️ 資材反映」で自分のスクラッチ/Sandbox に送って動作確認。
   環境側で変えたものは「⬇️ 資材取込」でローカルに戻す
4. **こまめにバックアップ** … 「③ 保存」の「💾 バックアップ」(commit + push)。1日数回でOK
5. **（必要なら）最新を取り込む** … 作業が長引いて \`develop\` が進んだら、「🌿 ブランチ管理」→ \`develop\` →
   「このブランチを現在のブランチに取り込む」で自分の feature に最新を取り込めます（競合が出たらホームの
   競合一覧で解決 → 「💾 バックアップ」で完了）。
6. **Pull Request を出す** … 「🔀 PR作成」で \`develop\` へ PR。CI が自動で検証
7. **レビュー → マージ** … 仲間がレビュー。マージされると統合環境へ自動デプロイ
   （feature を develop/main へ"統合"するのは PR 経由が基本。上記5のローカル取り込みは
   "自分のブランチを最新に追従させる"ためのものです）

## なぜ Git / GitHub なのか（バックアップとバージョン管理）

- **バックアップ**: ローカルだけだとPC故障で消えます。push すれば GitHub に安全に保管されます。
- **バージョン管理**: 「いつ・誰が・何を変えたか」が全部残る。壊れても前の状態に戻せます。
- **共同作業**: 同じ環境を複数人で直接いじると上書き事故が起きます。Git なら各自のブランチで作業し、
  PR でぶつかり（コンフリクト）を安全に解消できます。

### 複数人開発のおすすめルール
- \`main\` / \`develop\` には**直接 push しない**（必ず PR 経由）。GitHub の Branch protection を設定。
- PR は小さく・こまめに。レビューしやすく、コンフリクトも減ります。
- コミットメッセージは「何をしたか」を日本語で具体的に。
- 本番反映は**リリース担当者だけ**が \`main\` へマージ。本番デプロイ前には必ず確認が出ます。

## スクラッチ環境の活用（機能ごとの使い捨て環境）

スクラッチ = 使い捨ての開発用環境。**1機能=1ブランチ=1スクラッチ**で分けると、他の作業と干渉しません。

- 「🌱 スクラッチ作成」で数分で新しい環境が手に入る（最大30日）
- ローカルの資材を「⬆️ 資材反映」で送って試す
- 環境側で設定変更したら「⬇️ 資材取込」でローカルに戻す
- 機能が終わってマージしたら、その環境は捨てる（数に上限あり）

> 要 Dev Hub（スクラッチ作成元の環境）。ホームの「🌳 Dev Hub 準備」で本番/開発Edition環境を Dev Hub 化できます。

## メタデータの取得 (既存環境から始める)

すでにある環境から開発を始める場合:
1. 「📥 メタデータ取得」で Apexクラス・オブジェクト等を選んでローカルに取り込む
2. または \`manifest/package.xml\` を用意して取得
3. 取り込んだら「💾 バックアップ」で Git に保存してチームと共有

## 品質を保つ・元に戻す（便利な機能）
- **🤖 CI/CD生成**: PRで自動検証（check-onlyデプロイ＋Apexテスト＋**PMD静的解析**＋**LWC Jest**）、mergeで自動デプロイのGitHub Actionsを生成。
- **🔑 CI/CD設定**: 上を“実際に動く”状態にする。JWT鍵の生成＋各環境のGitHubシークレットを登録（接続アプリの作成手順も案内）。
- **🕘 変更履歴**: 「誰が・いつ・何を変えたか」を一覧 → コミットを選ぶと変更ファイルを確認。
- **⏪ 取り消し（ロールバック）**: 直近の変更を選んで安全に取り消す（履歴を壊さない＝後で戻せる）。間違えても安心。
- **🏷️ タグ管理**: リリースの目印。戻したいときの基準点になります。

## 困ったとき（よくあるエラーと対処）
- **「再接続して」/ INVALID_SESSION_ID / セッション切れ** … ブラウザのSalesforceログインが切れています。ホームの環境カードの「🔌再接続」を押す（またはブラウザで開き直して再ログイン）→「🔄 更新」。
- **GitHub公開/同期で「workflow scope」エラー** … CI/CDのワークフロー送信には gh の workflow 権限が必要。公開/同期の前に検知して案内が出るので「権限を追加する」を選び、ターミナルで認証を完了 → 再実行。
- **デプロイ/検証が失敗する（コンパイルエラー・テスト失敗）** … ホームの「🔎 実行ログ」で原因行を確認。先に「✅ 検証（お試し）」を実行すると、本番に触れずに失敗を洗い出せます。
- **同期・取り込みでコンフリクト** … 落ち着いて該当ファイルを開き、どちらの変更を残すか決めて保存（VSCodeの差分表示が使えます）。その後もう一度「💾 バックアップ」。
- **「sf が見つからない」** … Salesforce CLI が未インストール、またはPATH未設定です。CLIを入れるか、設定（⚙️）の「sf CLI のパス」に実行パスを指定。
- **「Missing message ... Finalizing」等の不可解なエラー** … sf CLI が古い可能性。ターミナルで \`sf update\` を実行して更新。
- **本番に出していいか不安** … まず「✅ 検証（お試し）」、もしくは本番デプロイ時に出る「先に検証する（おすすめ）」を選ぶ。
- **変更が見つからない/デプロイ対象が0件** … 基準ブランチ（既定 \`origin/main\`）との差分が無い状態です。コミット済みか、対象がパッケージディレクトリ内かを確認。
`;
